import { Injectable } from '@nestjs/common';

@Injectable()
export class CommentsService {
    // async createComment(createCommentDto: CreatePostCommentDto, postId: ObjectId, ): Promise<string> {

    // }
}
